[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["echo 'Skipping build — tsx runs TypeScript directly'"]

[start]
cmd = "cd apps/api && pnpm start"
